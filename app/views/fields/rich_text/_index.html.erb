<%#
# Rich Text Index Partial
This partial renders a rich text attribute
to be displayed on a resource's index page.
By default, the attribute is rendered as an unformatted string.
## Local variables:
- `field`:
  An instance of Administrate::Field::RichText.
  A wrapper around the rich text delta pulled from the database.
%>

<%= field.html_safe %>
